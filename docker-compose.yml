version: '3.8'

services:
  solr:
    image: solr:latest
    container_name: solr_nomologia
    ports:
      - "8983:8983"
    volumes:
      - ./solr_data:/var/solr
    command:
      - solr-precreate
      - nomologia